# Whisper.cpp Ruby Gem

This gem provides Ruby bindings for the `whisper.cpp` library, allowing you to transcribe audio files using the Whisper model.

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'whisper.cpp'

